import "./AvisosContenido.css"
import Aviso from "./Aviso.jsx"

export default function Avisos({avisos}) {
    const avisosArreglo = {
        mensaje: "mensaje"
    }

    
    return (
        <div id="contenidoAvisos">
            {/*avisosArreglo.map( aviso => <Aviso aviso={aviso} key={aviso._id} />)*/}
        </div>
    )
}